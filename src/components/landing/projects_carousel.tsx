import React, { ReactNode } from 'react';
import { NavLink } from "react-router-dom";
import '../../global.css'

export interface Props {
  }
  
const projects_carousel: React.FC<Props> = (props) => { 
    return (
      <h1 className='text-red-500 font-extrabold'>project project project</h1>
    )
}
export default projects_carousel;